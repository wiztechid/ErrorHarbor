(()=>{
const root=document.body.dataset.root||"";
const toast=document.createElement("div");toast.className="toast";toast.setAttribute("role","status");toast.setAttribute("aria-live","polite");document.body.appendChild(toast);
let tt;function say(msg){toast.textContent=msg;toast.classList.add("show");clearTimeout(tt);tt=setTimeout(()=>toast.classList.remove("show"),1800)}
document.querySelectorAll("[data-copy]").forEach(btn=>btn.addEventListener("click",async()=>{try{await navigator.clipboard.writeText(btn.dataset.copy);say("Copied")}catch(e){say("Copy ready")}}));
document.querySelectorAll("[data-example]").forEach(btn=>btn.addEventListener("click",()=>{const input=document.querySelector("[data-site-search] input");if(input){input.value=btn.dataset.example;input.focus()}}));
let searchIndex=null;
async function loadIndex(){if(searchIndex)return searchIndex;try{const r=await fetch(root+"data/search-index.json");searchIndex=await r.json();return searchIndex}catch(e){return[]}}
function esc(s){return String(s).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}
document.querySelectorAll("[data-site-search]").forEach(form=>form.addEventListener("submit",async e=>{e.preventDefault();const input=form.querySelector("input");const q=input.value.trim().toLowerCase();const box=document.querySelector("[data-search-results]");if(!q){say("Enter an error message first");return}const items=await loadIndex();const terms=q.split(/\s+/).filter(Boolean);const scored=items.map(x=>{const hay=(x.title+" "+x.keywords.join(" ")+" "+x.product).toLowerCase();return{x,score:terms.reduce((n,t)=>n+(hay.includes(t)?1:0),0)}}).filter(v=>v.score>0).sort((a,b)=>b.score-a.score).slice(0,6);if(box){box.innerHTML=scored.length?scored.map(v=>'<a href="'+root+v.x.url+'"><strong>'+esc(v.x.title)+'</strong><small>'+esc(v.x.product+" · "+v.x.type)+'</small></a>').join(""):'<a href="'+root+'errors/"><strong>No exact match yet</strong><small>Browse the Error Library while we expand the index.</small></a>';box.classList.add("active")}else if(scored[0])location.href=root+scored[0].x.url}));
document.querySelectorAll("[data-feedback]").forEach(btn=>btn.addEventListener("click",()=>{const block=btn.closest(".fix-feedback");const note=block?.querySelector("[data-feedback-note]");const detail=block?.querySelector(".feedback-detail");const key="eh:"+location.pathname+":"+btn.dataset.fix;const val=btn.dataset.feedback;try{localStorage.setItem(key,val)}catch(e){}if(note)note.textContent=val==="yes"?"Thanks — outcome saved on this device.":"Thanks — continue to the next diagnostic step.";if(detail)detail.classList.toggle("active",true)}));
const toc=[...document.querySelectorAll(".toc a")],sections=[...document.querySelectorAll(".article section[id]")];if(toc.length&&sections.length&&"IntersectionObserver"in window){const o=new IntersectionObserver(es=>es.forEach(en=>{if(en.isIntersecting)toc.forEach(a=>a.classList.toggle("active",a.getAttribute("href")==="#"+en.target.id))}),{rootMargin:"-25% 0px -62% 0px"});sections.forEach(s=>o.observe(s))}
})();